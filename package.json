{
  "name": "uk-esim-porter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx http-server -c-1 -p 5173 .",
    "preview:root": "http-server -p 5173 -a 0.0.0.0 -s .",
    "lint": "eslint .",
    "test": "jest --runInBand",
    "build": "echo 'Static app — nothing to build'"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "jest": "^29.7.0",
    "ajv": "^8.17.1",
    "http-server": "^14.1.1"
  }
}
